# Local dev can keep this empty to use Vite proxy (/api -> localhost:4000)
# For production (Vercel), set this to your Render backend URL
# Example: https://interfacehub-api.onrender.com/api
VITE_API_BASE_URL=
